## 2026-02-22 - Dynamic Form Accessibility
**Learning:** Arrays of inputs (like tool contracts) without visible labels rely on context visually, but are inaccessible to screen readers.
**Action:** Always add unique aria-labels (e.g., 'Tool 1 name') to dynamic inputs.
